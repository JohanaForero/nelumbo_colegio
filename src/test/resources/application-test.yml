spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver
    sql:
      init:
        mode: never

  jpa:
    show-sql: true
profiles:
  active: test